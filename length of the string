#include <stdio.h>

int main() {
    char string[100];
    int length = 0, i;

    printf("Enter a string: ");
    scanf("%s", string);

    for (i = 0; string[i] != '\0'; i++) {
        length++;
    }

    printf("Length of the string: %d", length);

    return 0;
}
